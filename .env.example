# Frontend: base URL for the backend API. Leave empty to use Vite's /api proxy.
VITE_API_BASE_URL=""

# Backend (PHP): MariaDB connection settings
DB_HOST="localhost"
DB_PORT=3306
DB_NAME="macerti_app"
DB_USER="macerti_app"
DB_PASSWORD="your_password_here"

# Data mode: demo (seeded demo providers) or real (no demo seed providers)
VITE_APP_DATA_MODE="demo"
